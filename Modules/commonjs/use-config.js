const config = require('./config'); // l'extention peut etre .js ou .json
console.log(config);


const configuration = require('./configuration'); // l'extention peut etre .js ou .json
console.log(configuration);
